name: material_neumorphic_repo
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ">=3.8.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  flutter_lints: ^6.0.0
  lints: ^6.1.0
  melos: ^7.5.1
  test: ^1.21.0
